const table = require('console.table');
const {
    renderDepartNew,
    renderRoleAdd,
    renderEmployeeUp,
    renderNewEmployee,
    renderRoleAll,
    renderDepartAll,
    renderEmployAll,
}   = require('../querydb');

renderDepartNew({id, department_name}){

}